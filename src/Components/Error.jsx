import { Alert } from "react-bootstrap";

const Error = () => (
  <Alert variant="danger">
    Error - indirizzo errato - controlla l'indirizzo o inserisci un altro nome
  </Alert>
);

export default Error;
